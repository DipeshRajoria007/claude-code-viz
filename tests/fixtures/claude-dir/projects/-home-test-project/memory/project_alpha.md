---
name: alpha-overview
description: "Alpha — uses bun:sqlite for: everything"
type: project
---

The alpha system overview. Builds on [[beta-feedback]] and references
[[missing-slug]] which doesn't exist anywhere.

```bash
# this fenced [[fenced-link]] must never become an edge
echo "[[fenced-link]]"
```

**Why:** alpha is the canonical test memory.

**How to apply:** see [[beta-feedback]] for the workflow.
